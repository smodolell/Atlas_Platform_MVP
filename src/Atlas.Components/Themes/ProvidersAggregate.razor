<MudThemeProvider IsDarkMode="@_appState.IsDark" Theme="@_appState.MudTheme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

@code {
    protected override void OnInitialized()
    {
        _appState.OnChange += StateHasChanged;
    }
}